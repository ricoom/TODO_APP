export class TodoDao {

    name:string;
    isComplete:boolean

    constructor(name:string, isComplete:boolean){
        this.name=name
        this.isComplete=isComplete
    }

    /**
     * name
     */
   
    }